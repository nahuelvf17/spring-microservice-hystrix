server:
  port: 9296

spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/nahuelvf17/config-server-hystrix
          default-label: main
          clone-on-start: true